slim.py is the script that writes the SMT model for the SLIM block cipher. The YAML file is used to define experimental parameters.

Both files are used with CryptoSMT (https://github.com/kste/cryptosmt).
